#main
  %table.recipes_table
    %caption
      Recipe details for
      %span.recipe= @recipe.name

    %tr.recipe_header
      %th Description
      %th Time
      %th Price

    %tr.product
      %td.recipedesc= @recipe.directions
      %td.recipetime= @recipe.cuisine
      %td.productprice= @recipe.calories

  =link_to("Back to recipe list", recipes_path)
